# `dev_docs/` in this repo

The layout follows [`dev_docs_layout.md`](../dev_docs_layout.md) at the plugin
root. This file lists only what this repo has.

| Path             | This repo keeps here                                                          |
| ---------------- | ----------------------------------------------------------------------------- |
| `conventions.md` | the live description of how the reviewing and authoring conventions are built |
| `designs/`       | dated records of each change to the plugin's shape                            |
| `.handoffs/`     | session-to-session notes, gitignored; its `README.md` says why it is tracked  |

No `decisions/`, `tasks/`, or `research/` yet. `co-review/` appears when the
co-review skill writes its local config; `.gitignore` already ignores it.
